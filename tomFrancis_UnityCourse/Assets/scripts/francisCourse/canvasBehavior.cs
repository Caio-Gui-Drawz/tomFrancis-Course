using UnityEngine;

public class canvasBehavior : MonoBehaviour
{
    // Awake acontece antes de start.
    void Awake()
    {
        references.Canvas = gameObject;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
